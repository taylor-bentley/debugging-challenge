
export function createMagicNumber(startingNumber){
  debugger;
    startingNumber ++;
    startingNumber *= 3
    startingNumber *= 5;
    startingNumber /= 2;
    startingNumber --;
    for(let i = 0; i < 333 ;i++)
    {
        startingNumber += 3;
    }
    return startingNumber;

}